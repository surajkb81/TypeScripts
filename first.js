var sub = function (x, y) {
    return (y - x);
};
console.log("the difference is ".concat(sub(3, 5)));
var add = function (a, b) {
    return (a + b);
};
console.log("The sum is ".concat(add(4, 5)));
console.log('The sum is ' + add(9, 8));
var empData = ['hello', 88, true, { dept: 1, deptName: 'it' }];
console.log('The array is ' + empData);

const sub = (x:number,y:number):number=>{
     return (y-x);
 }
 console.log(`the difference is ${sub(3,5)}`);

const add=(a,b)=>{
    return (a+b);
  }
  console.log(`The sum is ${add(4,5)}`);
  console.log('The sum is ' + add(9,8));
  
  let empData : [string,number,boolean,object]=['hello',88,true,{dept:1,deptName:'it'}];

  console.log('The array is ' + empData)
